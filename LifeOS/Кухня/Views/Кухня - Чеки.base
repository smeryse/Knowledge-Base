views:
  - type: table
    name: Таблица
    filters:
      and:
        - file.path.contains("LifeOS/Кухня/Receipts")
        - type == "receipt"
    sort:
      - property: file.name
        direction: DESC
      - property: date
        direction: DESC
    limit: 50
