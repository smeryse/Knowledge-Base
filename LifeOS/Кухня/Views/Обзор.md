---
tags:
  - еда
  - dashboard
aliases:
  - Dashboard
  - Дашборд еды
---

# Обзор

← [[Кухня]] | [[Views/Рецепты|Рецепты]] | [[Views/Запасы|Запасы]] | [[Views/Покупки|Покупки]] | [[Views/Готовка|Готовка]] | [[Views/План питания|План питания]] | [[Views/Чеки|Чеки]] | [[Views/Views|Views]] | [[Views/Food-Tasks|Food-Tasks]]

## Сводка

```dataviewjs
const products = dv.pages('"LifeOS/Кухня/Products"').where(p => p.type === "product");
const recipes = dv.pages('"LifeOS/Кухня/Recipes"').where(p => p.type === "recipe" && p.recipe_status !== "archived");
const receipts = dv.pages('"LifeOS/Кухня/Receipts"').where(p => p.type === "receipt");
const receiptItems = dv.pages('"LifeOS/Кухня/Receipt Items"').where(p => p.type === "receipt-item");
const pantry = dv.pages('"LifeOS/Кухня/Pantry"').where(p => p.type === "pantry-item");
const cooking = dv.pages('"LifeOS/Кухня/Cooking Log"').where(p => p.type === "cooking-entry");
const mealPlans = dv.pages('"LifeOS/Кухня/Meal Plans"').where(p => p.type === "meal-plan");
const shopping = dv.pages('"LifeOS/Кухня/Shopping List"').where(p => p.type === "shopping-item" && p.status != "done" && p.status != "cancelled");

function deriveExpiry(item) {
  const manufacturedOn = item.manufactured_on ? dv.date(item.manufactured_on) : null;
  const product = item.product?.path ? dv.page(item.product.path) : null;
  const shelfLifeDays = Number(product?.default_shelf_life_days || 0);
  if (!manufacturedOn || !shelfLifeDays) return null;
  return manufacturedOn.plus({ days: shelfLifeDays });
}

const today = dv.date('today');
const expiringSoon = pantry.where(p => {
  const expiresOn = deriveExpiry(p);
  return expiresOn && expiresOn <= today.plus({ days: 3 });
});

dv.table(["Показатель", "Значение"], [
  ["Товаров в базе", products.length],
  ["Рецептов", recipes.length],
  ["Чеков", receipts.length],
  ["Позиций в чеках", receiptItems.length],
  ["Запасов дома", pantry.length],
  ["Приготовлений", cooking.length],
  ["Планов питания", mealPlans.length],
  ["Пунктов к покупке", shopping.length],
  ["Скоро истекают", expiringSoon.length]
]);
```

---

## Последние чеки

```dataview
TABLE date as "Дата", store as "Магазин", total as "Сумма"
FROM "LifeOS/Кухня/Receipts"
WHERE type = "receipt"
SORT date DESC
LIMIT 10
```

---

## Последние добавленные товары

```dataview
TABLE category as "Категория", brand as "Бренд", barcode as "Штрихкод"
FROM "LifeOS/Кухня/Products"
WHERE type = "product"
SORT file.ctime DESC
LIMIT 15
```

---

## Последние рецепты

```dataview
TABLE dish_type as "Тип", servings as "Порций", total_time_min as "Мин"
FROM "LifeOS/Кухня/Recipes"
WHERE type = "recipe" AND recipe_status != "archived"
SORT file.ctime DESC
LIMIT 10
```

---

## Последняя готовка

```dataview
TABLE date as "Дата", recipe as "Рецепт", servings_cooked as "Порций"
FROM "LifeOS/Кухня/Cooking Log"
WHERE type = "cooking-entry"
SORT date DESC, file.mtime DESC
LIMIT 10
```

---

