views:
  - type: table
    name: Карточки
    filters:
      and:
        - type == "category"
        - file.path.contains("Projects/Кухня/Categories/")
    order:
      - file.name
    sort:
      - property: file.name
        direction: ASC
    markers: number
    indentProperties: false
