views:
  - type: cards
    name: Таблица
    filters:
      and:
        - file.path.contains("Projects/Кухня/Products")
        - type == "product"
    order:
      - aliases
    sort:
      - property: file.name
        direction: ASC
    image: note.image
