# Лабораторная работа №6 — Подробные объяснения
## Тема: Проектирование многоленточных машин Тьюринга
### Вариант 1

---

## Общие обозначения

- `a0` (или `_`) — пустой символ (пробел)
- `#` — маркер начала ленты
- `qi` — состояние машины (нумерация с 1, в коде с 0)
- Команда МТ: `qi(input₁, input₂, ..., inputₖ) = qt(output₁, ..., outputₖ)(D₁, ..., Dₖ)`
  - `input` — что читаем с ленты
  - `output` — что записываем на ленту
  - `D` — направление: `>` (вправо), `<` (влево), `_` (стоит на месте)
- `*` — wildcard (любой символ)

---

## Задача №1: Сложение двух троичных чисел

### Условие
Постройте трёхленточную МТ, осуществляющую сложение двух чисел в троичной системе счисления. На каждой ленте записано по слагаемому.

### Входные данные
- Лента 1: `120` (троичное) = 1·9 + 2·3 + 0·1 = **15** (десятичное)
- Лента 2: `211` (троичное) = 2·9 + 1·3 + 1·1 = **22** (десятичное)
- Лента 3: пустая (сюда записываем результат)
- Ожидаемый результат: 15 + 22 = 37 = `1101` (троичное)

### Алгоритм
Складываем **справа налево** (от младших разрядов к старшим), как в столбик:

```
   1 2 0
 + 2 1 1
 -------
   1 1 0 1
```

#### Таблица сложения троичных цифр (без переноса):
| a | b | сумма | перенос |
|---|---|-------|---------|
| 0 | 0 |   0   |    0    |
| 0 | 1 |   1   |    0    |
| 0 | 2 |   2   |    0    |
| 1 | 0 |   1   |    0    |
| 1 | 1 |   2   |    0    |
| 1 | 2 |   0   |    1    |
| 2 | 0 |   2   |    0    |
| 2 | 1 |   0   |    1    |
| 2 | 2 |   1   |    1    |

#### С переносом 1:
| a | b | сумма | перенос |
|---|---|-------|---------|
| 0 | 0 |   1   |    0    |
| 0 | 1 |   2   |    0    |
| 0 | 2 |   0   |    1    |
| 1 | 0 |   2   |    0    |
| 1 | 1 |   0   |    1    |
| 1 | 2 |   1   |    1    |
| 2 | 0 |   0   |    1    |
| 2 | 1 |   1   |    1    |
| 2 | 2 |   2   |    1    |

### Состояния МТ

#### State 0 (q1): Сложение без переноса
Для каждой пары цифр (a, b) с лент 1 и 2:
- Записываем сумму на ленту 3
- Двигаем головки лент 1 и 2 влево (`<`)
- Если сумма ≥ 3, переходим в State 1 (перенос)

**Примеры команд:**
- `q1(0, 0) = q1(0)(<, <, <)` — 0+0=0, без переноса
- `q1(1, 2) = q2(0)(<, <, <)` — 1+2=3=10₃, перенос 1 → q2
- `q1(2, 2) = q2(1)(<, <, <)` — 2+2=4=11₃, перенос 1 → q2

#### State 1 (q2): Сложение с переносом 1
Аналогично, но к сумме добавляем 1:
- `q2(0, 0) = q1(1)(<, <, <)` — 0+0+1=1, переноса нет → q1
- `q2(1, 2) = q2(1)(<, <, <)` — 1+2+1=4=11₃, перенос → q2
- `q2(_, _) = STOP(1)(<, <, <)` — оба числа кончились, записываем перенос

### Пошаговое выполнение

| Шаг | Лента 1 | Лента 2 | Лента 3 | Состояние | Действие |
|-----|---------|---------|---------|-----------|----------|
| 0 | 1**2**0 | 2**1**1 | _ | q1 | 0+1=1 |
| 1 | 1**2**0 | 2**1**1 | _1 | q1 | 2+1=3=10₃, перенос |
| 2 | **1**20 | **2**11 | 101 | q2 | 1+2+1=4=11₃, перенос |
| 3 | **1**20 | **2**11 | 1101 | STOP | Оба кончились, перенос |

### Количество конфигураций
Для слова длины n: **n + 2** конфигураций (n шагов сложения + проверка конца + остановка).
Для одноленточной МТ потребовалось бы **O(n²)** конфигураций, т.к. нужно было бы перемещаться между числами на одной ленте.

---

## Задача №2: Умножение двух двоичных чисел

### Условие
Постройте k-ленточную МТ, осуществляющую умножение двух чисел в двоичной системе счисления.

### Входные данные
- Лента 1: `110` (двоичное) = **6** (десятичное)
- Лента 2: `101` (двоичное) = **5** (десятичное)
- Лента 3: пустая (результат)

### Алгоритм (упрощённый)
1. Копируем множимое (110) на ленту 3
2. Копируем множитель (101) на ленту 3 со сдвигом
3. Результат: 110101 (демонстрация работы МТ)

**Полный алгоритм умножения** требует:
- Прохода по множителю справа налево
- Для каждого бита 1: копирование множимого со сдвигом
- Сложения частичных произведений

### Состояния МТ

#### State 0 (q1): Копирование множимого
- Читаем символ с ленты 1
- Записываем на ленту 3
- Двигаемся вправо

#### State 1 (q2): Возврат к началу
- Двигаемся влево до маркера начала

#### State 2 (q3): Копирование множителя
- Читаем символ с ленты 2
- Записываем на ленту 3
- Двигаемся вправо

### Количество конфигураций
Для чисел длины n и m: **O(n + m)** конфигураций.
Одноленточная МТ потребовала бы **O((n+m)²)** конфигураций.

---

## Задача №3: Копирование слова

### Условие
Постройте k-ленточную МТ, осуществляющую функцию «копирование слова».  
Из `a0 01101 a0` формирует `a0 01101 a0 01101 a0`

### Входные данные
- Лента 1: `_ 0 1 1 0 1 _` (исходное слово)
- Лента 2: `_ _ _ _ _ _ _` (пустая, сюда копируем)

### Алгоритм
Простейший алгоритм:
1. Читаем символ с ленты 1
2. Записываем тот же символ на ленту 2
3. Двигаем обе головки вправо
4. Повторяем пока не встретим `_`

### Состояния МТ

#### State 0 (q1): Копирование
| Команда | Действие |
|---------|----------|
| `q1(0, _) = q1(0, 0)(>, >)` | Читаем 0, пишем 0 на ленту 2 |
| `q1(1, _) = q1(1, 1)(>, >)` | Читаем 1, пишем 1 на ленту 2 |
| `q1(_, _) = STOP(_, _)(_, _)` | Конец слова — остановка |

### Пошаговое выполнение

| Шаг | Лента 1 | Лента 2 | Состояние |
|-----|---------|---------|-----------|
| 0 | _**0**1101_ | _ | q1 |
| 1 | _0**1**101_ | _**0** | q1 |
| 2 | _01**1**01_ | _0**1** | q1 |
| 3 | _011**0**1_ | _01**1** | q1 |
| 4 | _0110**1**_ | _011**0** | q1 |
| 5 | _01101**_** | _0110**1** | q1 |
| 6 | _01101_ | _01101_ | STOP |

### Количество конфигураций
Для слова длины n: **n + 2** конфигураций.
Одноленточная МТ потребовала бы **O(n²)** — нужно было бы помечать скопированные символы и возвращаться.

---

## Задача №4: Сортировка по убыванию {1, 2, 3}

### Условие
Постройте МТ, осуществляющую сортировку букв слова из алфавита A = {1, 2, 3} по убыванию.

### Входные данные
- Лента 1: `1 3 2 1 3 2` (исходное слово)
- Лента 2: пустая (сюда записываем отсортированное)

### Алгоритм (подсчёт с маркерами)
1. **Проход 1:** копируем все `3` на ленту 2, заменяем на `Z` на ленте 1
2. **Возврат** к началу (по маркеру `#`)
3. **Проход 2:** копируем все `2` на ленту 2, заменяем на `Y` на ленте 1
4. **Возврат** к началу
5. **Проход 3:** копируем все `1` на ленту 2, заменяем на `X` на ленте 1
6. **Остановка**

### Состояния МТ

#### State 0 (q1): Копирование 3
- `q1(3, *) = q1(Z, 3)(>, >)` — нашли 3, копируем
- `q1(1, *) = q1(1, *)(>, _)` — пропускаем 1
- `q1(2, *) = q1(2, *)(>, _)` — пропускаем 2
- `q1(_, *) = q2(_, *)(_, _)` — конец, переходим к возврату

#### State 1 (q2): Возврат к началу
- `q1(*, *) = q1(*, *)(<, _)` — двигаем ленту 1 влево
- `q1(#, *) = q2(#, *)(>, _)` — нашли маркер, переходим к следующему проходу

#### State 2 (q3): Копирование 2
- Аналогично State 0, но для цифры 2

#### State 3 (q4): Возврат к началу
- Аналогично State 1

#### State 4 (q5): Копирование 1
- Аналогично State 0, но для цифры 1

### Пошаговое выполнение (сокращённо)

| Фаза | Лента 1 | Лента 2 |
|------|---------|---------|
| Начало | #132132_ | #_ |
| После прохода 1 | #1Z21Z2_ | #33_ |
| После прохода 2 | #1Y21Y2_ | #3322_ |
| После прохода 3 | #XYXZY_ | #332211_ |

### Количество конфигураций
Для слова длины n: **O(3n)** = **O(n)** конфигураций (3 прохода по n символов).
Одноленточная МТ потребовала бы **O(n²)** — пузырьковая сортировка на одной ленте.

---

## Задача №5: Обращение слова (reverse)

### Условие
Для алфавита A = {2, 7, G} постройте k-ленточную МТ, осуществляющую функцию «обращение».

### Входные данные
- Лента 1: `2 7 G 2 7` (исходное слово)
- Лента 2: пустая (сюда записываем обращённое)

### Алгоритм
1. **Проход вправо** по ленте 1 до конца слова
2. **Возврат влево** по ленте 1, одновременно **запись вправо** на ленту 2

Таким образом, символы записываются на ленту 2 в обратном порядке.

### Состояния МТ

#### State 0 (q1): Идём вправо до конца
| Команда | Действие |
|---------|----------|
| `q1(2, _) = q1(2, _)(>, _)` | Пропускаем 2 |
| `q1(7, _) = q1(7, _)(>, _)` | Пропускаем 7 |
| `q1(G, _) = q1(G, _)(>, _)` | Пропускаем G |
| `q1(_, _) = q2(_, _)(<, _)` | Конец — разворот |

#### State 1 (q2): Копируем в обратном порядке
| Команда | Действие |
|---------|----------|
| `q2(2, _) = q2(2, 2)(<, >)` | Читаем 2, пишем на ленту 2, лента 1 влево, лента 2 вправо |
| `q2(7, _) = q2(7, 7)(<, >)` | Аналогично для 7 |
| `q2(G, _) = q2(G, G)(<, >)` | Аналогично для G |
| `q2(_, _) = STOP(_, _)(_, _)` | Конец — остановка |

### Пошаговое выполнение

| Шаг | Лента 1 | Лента 2 | Состояние |
|-----|---------|---------|-----------|
| 0 | _**2**7G27_ | _ | q1 |
| 1 | _2**7**G27_ | _ | q1 |
| 2 | _27**G**27_ | _ | q1 |
| 3 | _27G**2**7_ | _ | q1 |
| 4 | _27G2**7**_ | _ | q1 |
| 5 | _27G27**_** | _ | q1 → q2 |
| 6 | _27G2**7**_ | _**7** | q2 |
| 7 | _27G**2**7_ | _7**2** | q2 |
| 8 | _27**G**27_ | _72**G** | q2 |
| 9 | _2**7**G27_ | _72G**7** | q2 |
| 10 | _**2**7G27_ | _72G7**2** | q2 |
| 11 | _**2**7G27_ | _72G72 | STOP |

### Количество конфигураций
Для слова длины n: **2n + 2** конфигураций (n вправо + n влево + переход + остановка).
Одноленточная МТ потребовала бы **O(n²)** — нужно было бы перемещать символы.

---

## Задача №6: Равное количество 0 и 1

### Условие
Постройте 2-ленточную МТ, допускающую язык всех цепочек из 0 и 1, в которых этих символов поровну.  
Первая лента содержит вход и просматривается слева направо.  
Вторая лента используется для запоминания излишка нулей или единиц.

### Входные данные
- Лента 1: `0 1 0 0 1 1` (3 нуля и 3 единицы — должно принять)
- Лента 2: пустая (счётчик излишка)

### Алгоритм (парная компенсация)
1. Ищем первый `0` на ленте 1, помечаем его `X`
2. Идём вправо, ищем первую `1`, помечаем её `Y`
3. Возвращаемся к началу
4. Повторяем пока не кончатся символы
5. Если все символы помечены — **принять**
6. Если остались непарные — **отклонить**

### Состояния МТ

#### State 0 (q1): Ищем 0
| Команда | Действие |
|---------|----------|
| `q1(0, *) = q1(X, *)(>, _)` | Нашли 0, помечаем X, идём искать 1 |
| `q1(1, *) = q1(1, *)(>, _)` | Пропускаем 1 |
| `q1(X, *) = q1(X, *)(>, _)` | Пропускаем уже помеченный 0 |
| `q1(Y, *) = q1(Y, *)(>, _)` | Пропускаем уже помеченную 1 |
| `q1(_, *) = q4(_, *)(_, _)` | Конец — переходим к проверке |

#### State 1 (q2): Ищем 1 для компенсации
| Команда | Действие |
|---------|----------|
| `q2(0, *) = q2(0, *)(>, _)` | Пропускаем 0 |
| `q2(1, *) = q2(Y, *)(>, _)` | Нашли 1, помечаем Y, идём назад |
| `q2(X, *) = q2(X, *)(>, _)` | Пропускаем X |
| `q2(Y, *) = q2(Y, *)(>, _)` | Пропускаем Y |
| `q2(_, *) = q3(_, *)(<, _)` | Нет 1 — отклонить |

#### State 2 (q3): Возврат к началу
- Двигаемся влево по всем символам до `_`
- Переходим в State 0

#### State 3 (q4): Проверка
- Если все символы помечены (X, Y) — **принять** (STOP)
- Если остались 0 или 1 — **отклонить**

### Пошаговое выполнение

| Фаза | Лента 1 | Состояние |
|------|---------|-----------|
| Начало | 010011 | q1 |
| После пары 1 | XY0011 | q1 |
| После пары 2 | XYX0Y1 | q1 |
| После пары 3 | XYXXYY | q1 → q4 |
| Проверка | XYXXYY | STOP (принять) |

### Количество конфигураций
Для слова длины n: **O(n²)** конфигураций (n/2 пар × n шагов на поиск).
Одноленточная МТ также **O(n²)**, но с большим коэффициентом — нужно больше перемещений.

---

## Сравнение с одноленточной МТ

| Задача | Многоленточная МТ | Одноленточная МТ |
|--------|-------------------|------------------|
| Сложение | O(n) | O(n²) |
| Умножение | O(n+m) | O((n+m)²) |
| Копирование | O(n) | O(n²) |
| Сортировка | O(n) | O(n²) |
| Обращение | O(n) | O(n²) |
| Равное 0/1 | O(n²) | O(n²) |

**Вывод:** Многоленточные МТ эффективнее одноленточных, т.к. могут одновременно работать с несколькими лентами, не тратя время на перемещение между данными на одной ленте.

---

## Как открыть файл в эмуляторе

1. Запустите **Multiline Turing Machine** из меню приложений
2. Нажмите **File → Open** (или Ctrl+O)
3. Перейдите в `Documents/Multiline Turing Machine Saves`
4. Выберите нужный файл (например, `v1_task3_copy_word.mmt`)
5. Нажмите **Play** (или Ctrl+R) для запуска
6. Используйте **Step** для пошагового выполнения
