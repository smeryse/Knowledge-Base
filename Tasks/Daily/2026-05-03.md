---
date: 2026-05-03
---
*Воскресенье — нет занятий* | **Неделя:** I

---
## Задачи на сегодня
- [ ] Разделить почты и номера для важного и неважного
- [ ] сертификат форабанк 1000 руб озон (ДО «Ашан-Краснодар» улица Уральская, дом 79, помещение 23A.
- [ ] разобрать фотографии телефон (+290)
- [ ] Сделать прокси подключение к opencode openai api . Аналогично настроить использование впн на ноуте как на тел (анубис) 
## Перенесенные задачи
- [ ] начертить схему всех проектов (+ деньги + спорт)
- [ ] Нормализовать рецепт. [[Кухня]]
- [ ] Проблема: при составлениии рецептов указываются абстрактные продукты, а не ссылаются на конкретные. Как решить?
```dataviewjs
const file = dv.current().file.path;
const content = await dv.io.load(file);

const taskRegex = /-\s?\[([xX ])\].*?\(\+(\d+)\)/g;
const rubleRegex = /-\s?\[([xX ])\].*?\(\+(\d+)р\)/g;
let completed = 0, total = 0;

for (const match of content.matchAll(taskRegex)) {
    total += parseInt(match[2]);
    if (match[1].toLowerCase() === 'x') completed += parseInt(match[2]);
}

// 5% от заработанных рублей → баллы
for (const match of content.matchAll(rubleRegex)) {
    const rublePoints = Math.round(parseInt(match[2]) * 0.05);
    total += rublePoints;
    if (match[1].toLowerCase() === 'x') completed += rublePoints;
}

const width = 50;
const filled = Math.round((completed / total) * width);
const percent = Math.round((completed / total) * 100);
const bar = '█'.repeat(filled) + '░'.repeat(width - filled);

dv.paragraph(`**Прогресс:** \` ${bar} \` **${completed}/${total}** (${percent}%)`);
```


---
## Траты

---
## Накопления