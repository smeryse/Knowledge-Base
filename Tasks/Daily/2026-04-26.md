---
date: 2026-04-22
---
## 📅 Обновлённое расписание

| Время             | Действие                    | Комментарий                |
| ----------------- | --------------------------- | -------------------------- |
| **03:00 – 10:00** | Сон (уже был)               | Нормальная база            |
| **10:00 – 11:00** | Подъём, еда, раскачка       | Без тяжести                |
| **11:00 – 14:30** | Готовка + спокойные дела    | Сделай еду на смену и утро |
| **15:00 – 19:00** | **Основной сон (КРИТИЧНО)** | Теперь это главный якорь   |
| **19:00 – 19:40** | Подъём, еда, кофе           | Включиться перед сменой    |
| **20:00 – 00:00** | Работа                      | Начало смены               |
| **00:00 – 02:00** | Работа + еда                | Основной приём пищи        |
| **02:00 – 04:00** | Работа + перекус            | Последний кофе             |
| **04:00 – 08:00** | Работа                      | Самый тяжёлый блок         |
| **08:00 – 09:00** | Дорога + лёгкая еда         | Не переедай                |
| **09:30 – 11:30** | **Короткий сон**            | Обязательно                |
| **12:40 – …**     | Пары                        | На “остатках энергии”      |
## Задачи на сегодня

## Перенесенные задачи

```dataviewjs
const file = dv.current().file.path;
const content = await dv.io.load(file);

const taskRegex = /-\s?\[([xX ])\].*?\(\+(\d+)\)/g;
const rubleRegex = /-\s?\[([xX ])\].*?\(\+(\d+)р\)/g;
let completed = 0, total = 0;

for (const match of content.matchAll(taskRegex)) {
    total += parseInt(match[2]);
    if (match[1].toLowerCase() === 'x') completed += parseInt(match[2]);
}

// 5% от заработанных рублей → баллы
for (const match of content.matchAll(rubleRegex)) {
    const rublePoints = Math.round(parseInt(match[2]) * 0.05);
    total += rublePoints;
    if (match[1].toLowerCase() === 'x') completed += rublePoints;
}

const width = 50;
const filled = Math.round((completed / total) * width);
const percent = Math.round((completed / total) * 100);
const bar = '█'.repeat(filled) + '░'.repeat(width - filled);

dv.paragraph(`**Прогресс:** \` ${bar} \` **${completed}/${total}** (${percent}%)`);
```

---
## Траты

---
## Накопления
- [x] работа ozon fresh 20:00-8:00 (+3663р) ✅ 2026-04-27