---
date: 2026-04-22
---
*Воскресенье — нет занятий* | **Неделя:** II
### 📅 План на ближайшие ~36 часов

| Время             | Действие                       | Комментарий                    |
| ----------------- | ------------------------------ | ------------------------------ |
| **02:00 – 05:30** | Сон (короткий)                 | Минимально восстановиться      |
| **05:30 – 06:00** | Подъём, свет, вода, завтрак    | Разбудить организм             |
| **06:00 – 10:00** | Готовка еды                    | На смену + на утро после неё   |
| **10:00 – 12:30** | Спокойные дела / отдых         | Не перегружаться               |
| **13:30 – 18:30** | **Основной сон (ОБЯЗАТЕЛЬНО)** | Ключевой блок перед сменой     |
| **18:30 – 19:30** | Подъём, еда, сборы             | Можно кофе                     |
| **20:00 – 00:00** | Работа                         | Начало смены                   |
| **00:00 – 02:00** | Работа + основной приём пищи   | Поддержка энергии              |
| **02:00 – 04:00** | Работа + лёгкий перекус        | Кофеин — последний             |
| **04:00 – 08:00** | Работа (самый тяжёлый период)  | Без кофе, больше движения      |
| **08:00 – 09:00** | Дорога домой + лёгкая еда      | Не переедать                   |
| **09:30 – 11:30** | **Короткий сон (критично)**    | 1.5–2 часа спасут состояние    |
| **11:30 – 12:00** | Подъём, душ, кофе              | Быстро привести себя в форму   |
| **12:40 – …**     | Пары                           | Будешь уставший, это нормально |
## ⚠️ Ключевые точки

- **13:30–18:30 сон — самый важный блок**
    
- **09:30–11:30 сон — второй по важности**
    
- После 04:00 **не пей кофе**, иначе не уснёшь перед парами

## Задачи на сегодня
- [ ] КАСД Контест CT9 — задачи 4-7 (+20)

## Перенесенные задачи

```dataviewjs
const file = dv.current().file.path;
const content = await dv.io.load(file);

const taskRegex = /-\s?\[([xX ])\].*?\(\+(\d+)\)/g;
const rubleRegex = /-\s?\[([xX ])\].*?\(\+(\d+)р\)/g;
let completed = 0, total = 0;

for (const match of content.matchAll(taskRegex)) {
    total += parseInt(match[2]);
    if (match[1].toLowerCase() === 'x') completed += parseInt(match[2]);
}

// 5% от заработанных рублей → баллы
for (const match of content.matchAll(rubleRegex)) {
    const rublePoints = Math.round(parseInt(match[2]) * 0.05);
    total += rublePoints;
    if (match[1].toLowerCase() === 'x') completed += rublePoints;
}

const width = 50;
const filled = Math.round((completed / total) * width);
const percent = Math.round((completed / total) * 100);
const bar = '█'.repeat(filled) + '░'.repeat(width - filled);

dv.paragraph(`**Прогресс:** \` ${bar} \` **${completed}/${total}** (${percent}%)`);
```


---
## Траты

---
## Накопления
- [ ] работа ozon fresh 20:00-8:00